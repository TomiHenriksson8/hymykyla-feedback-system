
# --- Database ---
DB_USER=db_user
DB_PASS=your_db_password_here
MONGODB_URI=mongodb+srv://${DB_USER}:${DB_PASS}@cluster0.dwr947f.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# --- Server ---
PORT=8080
NODE_ENV=development

# --- CORS ---
# Frontend origin (Vite dev server by default). Change if frontend running diff port
CORS_ORIGIN=http://localhost:5173

# --- Auth / Sessions ---
JWT_SECRET=change_this_later_to_a_long_random_string
COOKIE_NAME=hymy_admin

# --- Admin create (for first login in dev) ---
ADMIN_EMAIL=admin@hymyk.local
ADMIN_PASSWORD=admin1234
